{
  "version": 2,
  "builds": [
    {
      "src": "white_board_backend/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "white_board_backend/wsgi.py" }
  ]
}
